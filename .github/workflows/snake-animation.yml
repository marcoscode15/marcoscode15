name: Snake Animation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Snake Animation
        uses: Platane/snk@master
        with:
          github_user_name: marcoscode15 # Substitua pelo seu nome de usuário do GitHub
          svg_out_path: dist/github-contribution-grid-snake.svg
